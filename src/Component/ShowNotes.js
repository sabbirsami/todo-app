import React from "react";

const ShowNotes = ({ note }) => {
    return (
        <div>
            <>
                <h1>{note}</h1>
            </>
        </div>
    );
};

export default ShowNotes;
